@model IEnumerable<ZambaFarm.Models.Pig>

@{
    ViewData["Title"] = "Pig Dashboard";
}

<div class="container mt-5">
    <!-- Page Header -->
    <div class="row justify-content-between align-items-center mb-4">
        <div class="col-md-8">
            <h2 class="text-primary">Pig Dashboard</h2>
            <p class="text-muted">Overview of all registered pigs</p>
        </div>
        <div class="col-md-4 text-md-end">
            <a asp-action="Create" class="btn btn-success"><i class="bi bi-plus-circle me-2"></i>Add New Pig</a>
        </div>
    </div>

    <!-- Table for Pig Data -->
    <div class="table-responsive">
        <table class="table table-hover table-bordered align-middle">
            <thead class="table-dark text-center">
                <tr>
                    <th>#</th>
                    <th>Tag Number</th>
                    <th>Gender</th>
                    <th>Birth Date</th>

                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="6" class="text-center text-muted">No pigs found.</td>
                    </tr>
                }
                else
                {
                    @foreach (var pig in Model)
                    {
                        <tr>
                            <td class="text-center">@pig.PigId</td>
                            <td>@pig.TagNumber</td>
                            <td>@pig.Gender</td>
                            <td>@pig.BirthDate.ToShortDateString()</td>

                            <td class="text-center">
                                <div class="d-flex justify-content-center">
                                    <a asp-action="Details" asp-route-id="@pig.PigId" class="btn btn-info btn-sm me-2">
                                        <i class="bi bi-info-circle"></i> Details
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@pig.PigId" class="btn btn-warning btn-sm me-2">
                                        <i class="bi bi-pencil"></i> Edit
                                    </a>
                                    <form asp-action="Delete" asp-route-id="@pig.PigId" method="post" class="d-inline">
                                        <button type="submit" class="btn btn-danger btn-sm">
                                            <i class="bi bi-trash"></i> Delete
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
